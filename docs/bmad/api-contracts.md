# API Contracts — svicloudtvbox.us

Single part (root). See `./api-contracts-root.md`.

## Summary
- No custom REST/GraphQL endpoints in this repo.
- Theme uses WordPress/WooCommerce built-in endpoints/AJAX (e.g., `wc-ajax=add_to_cart`, `admin-ajax.php`).
- No OpenAPI/GraphQL specs present.
