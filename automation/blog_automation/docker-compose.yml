version: "3.9"
services:
  autoblog:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./drafts:/app/drafts
    command: ["--max-posts", "1"]
